# 정확도·실시간성 코드 검토 (2026-09-05)

모델을 바꾸지 않아도 개선할 여지가 있다. 아래 최초 검토는 Google Drive 배포 당시의 코드에 대한 제안이다. 후속 요청에 따라 전문용어 사전은 제외하고 다음 항목을 구현했다. 전공이 다른 사용자에게 공통 사전이나 공통 전공 문맥을 적용하지 않는다.

## 후속 구현 현황

- **CLOVA 안전 재시도·결과 회수:** 처리 전 admission 429에만 `chunk_not_started`와 명시적 안전 표시를 붙인다. 응답이 유실되면 소유자·음성 SHA-256·시간축·final이 일치하는 기존 결과를 인증된 GET으로 회수한다. 처리 중이면 제한된 횟수·시간 동안 GET만 반복하고, 결과 없음·조회 실패·구버전 API는 수동 확인으로 남긴다. 서버 재시작으로 미확정 행이 없어져도 이를 재전송 허가로 해석하지 않는다.
- **확정 결과 선표시:** 응답과 계정·수업 연결을 검증한 뒤 텍스트를 표시하고 IndexedDB ACK/삭제를 수행한다. ACK 실패 때도 음성은 남기고, 재실행 후 서버 확정 결과를 확인한 경우에만 blocked 음성을 원자적으로 ACK할 수 있다.
- **긴 수업 화면:** 같은 화면 범위의 변경 없는 segment DOM을 재사용하고, 정상 시간 순서의 새 결과는 전체 정렬을 생략한다. 전체 segment 탐색 자체를 없앤 가상 스크롤 구현은 아니다.
- **Qwen 경계 보강·구두점:** 마지막 6초·최대 96토큰·48KiB의 단어 시각과 확정 여부를 수업별 private DB에 저장한다. 문맥은 해당 chunk의 WAV·확정 텍스트와 함께 commit하고 공유 모델 객체에는 보관하지 않는다. ±0.2초 정렬 흔들림의 누락 조건과 두 개의 서로 다른 인접 단어가 일관된 시간 이동을 보이는 중복 조건을 회귀로 검사한다. 단독 동일 단어는 실제 새 반복과 구분할 수 없어 보존한다. 문장 끝 구두점과 완전한 인용부호도 보존한다.
- **CLOVA 동시 처리:** 원격 CONFIG/ACK 응답 대기를 수업별로 분리하고 같은 소유자·수업은 FIFO 순서를 유지한다. 생성 중 세션 예약·취소·busy 세션 정리 제외·응답 순번·대기표 정리를 검사한다. 로컬 Qwen GPU와 앱 전체 처리량 제한은 유지한다.
- **무음 연속 녹음·수동 일시정지:** 이전 미배포 체크포인트의 무음 자동 휴식·자동 재개는 사용자 요청으로 완전히 제거했다. 조용한 구간도 PCM과 수업 시간축에 보존하며, 수동 일시정지·재개와 명시적 종료는 유지한다. 이는 OS가 입력을 중단한 동안의 음성까지 복구한다는 뜻이 아니다.
- **같은 탭 로그인 유지:** 최대 24시간/서버 만료 중 빠른 시각까지 `sessionStorage`를 사용하고 같은 검증된 서버의 `/auth/me`로 복구한다. 녹음 자동 재시작과는 별개이며 계정·서버 소유권 경계를 유지한다.
- **원문과 별도의 AI 요약·문맥 번역:** 최종 저장된 원문만 처리한다. 요약은 개요·핵심 내용·복습 질문과 원문 참조를 만들고, 번역은 각 원문 구간의 ID·시간·순서·개수를 유지한다. 번역은 전체 수업의 모든 묶음에서 추린 최대 2,400자 공통 문맥과 앞뒤 두 구간을 각 번역 요청에 사용한다. 문장 대조와 한국어 전체 보기는 같은 결과의 표시 방식이며 별도 요약이나 추가 모델 호출이 아니다. 숫자·일부 개인정보·기존 한국어를 보호하고 처리 전후 원문 revision을 검사하지만, 의미의 정확성까지 보장하지는 않는다.

**보류:** 전문용어 사전은 사용자 요청으로 제외했다. CLOVA의 더 작은 음성 전송 단위·별도 발화 확정·중간 결과 UI는 내구 수신 대기열과 프로토콜 변경이 필요해 이번에 적용하지 않았다. 브라우저의 8~15초 음성 수집 정책과 매 청크의 epFlag 확정은 그대로다. 파일 전용 긴 문맥, 마이크 처리 옵션, 미완성 PCM 내구 저장, Drive 잠금 축소도 후속 평가 대상으로 남긴다.

위 구현은 합성 결과·임시 DB·가짜 전송기 회귀에 더해 아래 짧은 실제 모델·브라우저 시험으로 확인했다. 실제 교실 정확도 상승률이나 학교 기기의 자막 지연 감소량을 측정한 결과는 아니다. 구현 커밋 `73d2a2f`의 서버 재시작·schema 12 이전·Pages 배포를 완료했고 외부 접속 및 공개 자산 10개의 소스 일치를 확인했다. 자세한 운영 결과는 HANDOFF의 최신 체크포인트를 따른다.

## 최신 작업 트리의 실제 검증과 한계

문서 정리 시점의 전체 회귀는 Python 408개(80.109초), Node 199개(3.731초), 총 607개 통과다. 후속 수정·최종 배포 결과는 HANDOFF를 기준으로 한다.

| 실행 범위 | 결과 | 주장할 수 없는 것 |
| --- | --- | --- |
| Qwen 최신 수업별 경계 문맥, 공개 FLEURS 113.37초·13청크 | CER 4.03%, 마지막 기준 25자 일치, 중복 탐지 0. 추론 166.276초·RTF 1.467, RSS +10.91 MiB, GPU allocated +0 MiB·reserved +6 MiB. | 이번 실행은 입력보다 느렸으며 이전 RTF 0.466보다 속도가 개선됐다고 말할 수 없다. 교실 정확도 상승·항상 실시간 처리·긴 수업 무누락/무중복도 입증하지 않는다. |
| CLOVA 공개 음성 반복·무음 삽입, 입력 270.21초·wall 267.372초·34청크 | 자연 경과 248초의 스트림 교체, 256.3초 시험 연결 강제 종료 뒤 재연결, 최종 세션·reader thread 0. CER 3.92%, 마지막 기준문 CER 3.03%. 반복 8-gram 초과 후보 87. | 마지막 문장은 완전 일치가 아니다. 반복 코퍼스에서 얻은 87은 중복 후보 지표라 실제 오류 건수로 볼 수 없고, 텍스트 중복이 없다고도 주장하지 않는다. 학교 네트워크나 45~90분 수업 시험은 아니다. |
| 실제 Chromium·loopback HTTP·합성 입력·가짜 ASR/LLM | 13.589초 무음 녹음 유지, AudioWorklet, 오프라인 IndexedDB 보존·재전송, 수동 pause/resume·final, 새로고침 로그인, 요약/번역/Markdown·로그아웃·다른 시험 계정 격리 통과. 저장 WAV 16.361375초·3개 청크. | 실제 마이크 음질·모델 품질, 학교 Wi-Fi·태블릿·화면 잠금, 장시간 메모리나 운영 계정의 외부 end-to-end를 대신하지 않는다. |

Qwen 재현 스크립트는 이제 `start_seconds/boundary_context/boundary_output`을 실제로 전달하며 `--boundary-context`가 기본이다. 이전 호출은 새 경계 함수를 건너뛰었으므로 그 결과를 최신 구현의 검증으로 재사용하지 않는다. 위 Qwen과 CLOVA는 음성 구성·길이와 실행 조건이 달라 CER만으로 보편적인 품질 순위를 매기지 않는다.

NOVA 비개인 한국어 예문의 실제 후보정은 1회·14.117초, 요약은 1회·9.005초(5주제·복습 3질문)로 검증했다. 영어 문맥 번역도 합성 예문 10개 구간을 7회 호출·15.391초에 처리해 통과했다. 후반의 강둑 정의를 포함한 전체 문맥에서 첫 `bank`가 강둑/제방으로 번역됐고, 숫자 `15` 및 원문의 ID·시간·순서·개수는 보존됐다. 초기 실패는 개요의 명백한 연속 목록 표식 `(1)…(n)`만 정리하고 번역 대상 `segments`와 읽기 전용 `readonly_context`를 분리한 뒤, 응답 스키마를 정확한 대상 ID·개수로 제한해 해결했다. 원문·숫자·개인정보 보호 규칙은 완화하지 않았다. 전체 수업의 요약·번역 품질은 별도의 기준문·의미 대조가 필요하며, 이 짧은 시험만으로 문맥 처리의 일반적인 정확도 향상을 입증하지 않는다.

## 최초 검토의 우선순위와 근거 (변경 전 코드)

| 순서 | 후보 | 검토 당시 코드에서 확인한 점 | 구현 시 지킬 조건 |
| --- | --- | --- | --- |
| 1 | CLOVA의 안전한 자동 복구 | `web/app.js`의 `flushPending()`은 일반 CLOVA 오류를 수동 재시도로 막는다. `server/app.py`가 provider 전송 **전** 반환하는 capacity 429도 포함되어 뒤의 큐가 기다린다. 마이크 캡처가 종료되는 것과는 다르다. | 서버에서 전송 전 거절과 전송 후 응답 불명을 구분해야 한다. owner 검사된 청크 상태 조회로 `done` 결과를 회수하고 `pending`은 기다린다. 재시작 때 pending을 지우는 현 정책도 고려해 provider 전송 이력을 영속화해야 하며, 불명확한 요청을 무조건 재전송하지 않는다. |
| 2 | 수업별 전문용어 사전 | `server/clova_transcriber.py`의 CONFIG에는 언어와 EPD만 있으며 `keywordBoosting`은 없다. | 수업 소유자만 설정·조회하고 수업 시작 시 스냅샷을 사용한다. 용어·사전은 공개 프런트엔드나 Git에 넣지 않는다. 부스팅 전후 같은 음성으로 전문용어 오류와 잘못 삽입된 용어를 모두 비교한다. |
| 3 | 확정 결과 먼저 표시 | `web/app.js`는 성공 응답을 merge한 뒤 `acknowledgePendingChunk()`의 IndexedDB 삭제를 기다린 다음 `renderCurrent()`를 호출한다. 삭제는 재시도·시간 제한을 포함한다. | 검증된 응답은 먼저 표시하되 영속 ACK 실패 상태·재실행 복구·chunk ID 기반 중복 방지는 유지한다. 일반 환경에서 절약되는 시간은 아직 측정하지 않았다. |
| 4 | Qwen 경계 누락·중복 보강 | `server/transcriber.py`는 독립 청크의 forced alignment 중앙 시각을 `overlap−0.6`부터 `duration−0.6`까지 선택한다. 양쪽 정렬 시각이 흔들리면 누락·중복 가능성이 있다. | 수업별 텍스트·시각 문맥으로 경계를 대조하고 불확실한 tail을 유예한다. 실제 반복 발화를 중복으로 지우면 안 된다. 가드 수치만 늘리는 것으로 해결된다고 가정하지 않는다. |
| 5 | CLOVA 전송·확정 단위 분리 | `web/audio.js`는 CLOVA도 8초 새 음성·최대 15초 WAV·3초 overlap 정책으로 모은다. 서버는 매 HTTP 청크 끝에서 `epFlag=true`와 seq ACK를 사용한다. | 작은 PCM 전송 단위와 발화 확정을 분리하고 중간 결과는 같은 영역을 교체, 확정 결과만 영속 저장한다. 기존 큐·ACK·이전 문맥·마지막 tail·4분 rotation 복구를 보존한다. 단순히 모든 청크를 1초로 줄이는 변경은 권하지 않는다. |
| 6 | 긴 수업 화면 증분 갱신 | `mergeChunkSegments()`는 전체 ID 집합·정렬, `renderTranscript()`는 전체 DOM 삭제·재생성을 한다. | 추가·수정된 segment만 갱신한다. 선택한 지난 수업·후보정·스크롤 유지도 함께 테스트한다. 실제 태블릿에서의 지연 영향은 측정이 필요하다. |

CLOVA의 `keywordBoosting`과 `epFlag` 의미는 [NAVER Cloud 공식 gRPC API](https://api.ncloud-docs.com/docs/ai-application-service-clovaspeech-grpc)를 확인했다. 직접 단어 목록과 도메인 부스팅 ID는 동시에 사용할 수 없다. 현재 8~15초는 코드의 입력 수집 정책이지 측정된 종단 지연이나 CLOVA 자체의 추론 지연 수치가 아니다.

## 최초 코드에서 재현한 Qwen 경계 조건

가짜 모델·가짜 정렬 결과를 사용한 재현이다. 실제 녹음에서의 발생률을 의미하지 않는다. 첫 청크 8초, 다음 청크의 시작은 5초, overlap 3초, guard 0.6초일 때:

| 같은 단어의 두 정렬 중앙 시각 | 첫 청크 선택 | 다음 청크 선택 | 결과 |
| --- | --- | --- | --- |
| 첫 7.5초 / 다음 2.3초 | 7.4초 이상이므로 제외 | 2.4초 미만이므로 제외 | 0회: 누락 |
| 첫 7.3초 / 다음 2.5초 | 포함 | 포함 | 2회: 중복 |

당시 `tests/test_transcriber.py`는 정렬 시각이 정확히 맞는 겹침을 주로 검사했다. 이후 `tests/test_qwen_boundary.py`와 경계 API 회귀에 ±0.2초 흔들림·실제 반복 발화 보존·수업별 문맥과 같은 transaction 저장 조건을 추가했다. 위 표는 변경 전 재현 근거이지 최신 코드에서 모든 조건이 그대로 실패한다는 뜻이 아니다.

## 최초 후순위 후보와 후속 적용 구분

- **Qwen 구두점 보존 — 적용:** 당시 `aligned_text_slice()`가 양끝 `.?!`를 제거해 `맞나요?`가 `맞나요`로 바뀌었다. 후속 구현에서 잘린 경계와 확정 문장 끝을 구분해 구두점을 보존했다.
- **파일 전용 더 긴 문맥:** `server/importer.py`도 실시간과 같은 8~15초 분할을 사용한다. 기존 README의 같은 FLEURS 10개 결과는 전체 파일 CER 2.24%, 분할 4.03%다. 문장·묵음 기준 긴 구간을 비교할 이유는 있지만, 짧은 낭독·서로 다른 처리 경로의 결과이므로 실제 수업 개선량으로 일반화하지 않는다. `max_new_tokens`, 음성 길이·업로드 제한·메모리를 함께 검토해야 한다.
- **CLOVA 수업별 잠금 — 적용:** 당시 전역 `_operation_lock`이 원격 응답 대기까지 감싸 다른 사용자가 기다렸다. 후속 구현에서 수업별 FIFO와 짧은 전역 관리 잠금으로 분리하고 공용 처리 한도는 유지했다. 로컬 Qwen GPU 직렬화와는 별개다.
- **마이크 프로필:** echo cancellation/noise suppression/AGC와 조용한 경계 RMS가 고정되어 있다. 먼 교실 목소리에서 처리 옵션과 적응형 경계를 비교할 수 있으나, 잡음 억제를 끄면 더 정확하다고 단정하지 않는다.
- **미완성 음성 내구 저장:** IndexedDB 저장은 완성 WAV 단위다. visibility/pagehide 처리는 있지만 강제 종료 때 마지막 미완성 PCM까지 보장하지 못한다. 인식 단위는 유지하면서 더 작은 PCM 조각을 저장하는 별도 경로가 후보다. OS가 마이크 자체를 멈춘 동안의 음성은 복구할 수 없다.
- **Drive 잠금 범위:** 전체 녹음 업로드와 다운로드 시작이 같은 전역 archive 잠금을 사용한다. 큰 업로드 중 다른 파일 다운로드·삭제가 기다릴 수 있다. ASR 추론 잠금과는 별개이며, 잠금을 줄일 때 삭제·업로드 응답 유실의 경쟁 안전성을 다시 검증해야 한다.

## 최초 검토 범위와 다음 평가

최초 검토는 코드·공식 문서 확인과 합성 정렬 조건 재현만 수행했고 당시에는 새 ASR 설정의 실음성·유료 API를 호출하지 않았다. 그 뒤 실행한 실제 시험은 위 최신 검증 절과 구분한다. 자동 테스트 통과나 짧은 공개 음성 실행이 실제 수업의 정확도 개선을 입증하지는 않는다.

다음 비교에는 사용 허락을 받은 실제 수업 5~10분과 가능한 범위의 기준문이 필요하다. 계정·이름·민감한 발언은 공개 산출물에 넣지 않는다. 같은 음성으로 CER, 전문용어 오류, 경계 누락·중복, 발화 종료부터 화면 확정까지 p50/p95, 서버/브라우저 메모리, 단절 후 복구를 비교한다. 최신 CLOVA의 자연 경과 4분 교체는 짧게 확인했지만 반복 교체·45~90분 수업, 실제 학교 Wi-Fi/Chrome/태블릿 백그라운드와 종료 tail은 별도 현장 검증이 필요하다.
