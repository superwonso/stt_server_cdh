fastapi==0.141.1
uvicorn[standard]==0.52.4
argon2-cffi==25.1.0
python-dotenv==1.2.3
numpy==2.4.6
qwen-asr==0.0.6
webrtcvad-wheels==2.0.14
httpx==0.28.1
# Used directly for bounded, streaming decode of uploaded recordings.  Keep it
# explicit even though qwen-omni-utils currently brings it in transitively.
av==18.1.0
